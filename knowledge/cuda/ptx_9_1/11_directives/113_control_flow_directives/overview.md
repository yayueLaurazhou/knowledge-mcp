# 11.3. Control Flow Directives

## 11.3. [Control Flow Directives](https://docs.nvidia.com/cuda/parallel-thread-execution/#control-flow-directives)[](https://docs.nvidia.com/cuda/parallel-thread-execution/#control-flow-directives "Permalink to this headline")

PTX provides directives for specifying potential targets for `brx.idx` and `call`
instructions. See the descriptions of `brx.idx` and `call` for more information.

* `.branchtargets`
* `.calltargets`
* `.callprototype`