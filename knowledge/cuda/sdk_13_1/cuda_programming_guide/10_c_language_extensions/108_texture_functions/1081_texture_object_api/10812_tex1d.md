# 10.8.1.2. tex1D()

#### 10.8.1.2. tex1D()[](#tex1d "Permalink to this headline")

```
template<class T>
T tex1D(cudaTextureObject_t texObj, float x);
```

fetches from the CUDA array specified by the one-dimensional texture object `texObj` using texture coordinate `x`.