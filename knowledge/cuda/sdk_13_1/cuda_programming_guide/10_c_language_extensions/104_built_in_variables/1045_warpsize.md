# 10.4.5. warpSize

### 10.4.5. warpSize[](#warpsize "Permalink to this headline")

This variable is of type `int` and contains the warp size in threads (see [SIMT Architecture](#simt-architecture) for the definition of a warp).